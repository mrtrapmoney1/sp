Partlog:
	4/28/25 Added Doy of Year to the unique time field

	Future: Add blue field with DOY on Bad Eyes

	Trying to get leading zeros to show in datekey field
	DOY x 100,000,000
	hours x 1,000,000
	minutes x 10,000
	seconds x 100
	hundredths x1

	NEW FORMULA 4/29/25
	DayOfYear(Partlog.CREDATE) * 100000000 + Hour(Partlog.CRETIME) * 1000000 + Minute(Partlog.CRETIME) * 10000 + Second(Partlog.CRETIME) * 100 + Hundredth(Partlog.CRETIME)

	DEPRECIATED FORMULA 4/29/25
	Copied existing formula for datekey 
	Combine(DayOfYear(Partlog.CREDATE), Hour((Partlog.CRETIME)), Minute((Partlog.CRETIME)), Second((Partlog.CRETIME)), Hundredth((Partlog.CRETIME)))

	5/1 Change DateKey to Numeric 12.0 Modification FORMULA, added last digit of year to string, 
	
	5/2 Made TimeKey to be unique and filled in. Removed unique requirement for cretime field. 
	
	Current formula for TimeKey field is: Right(Year(Partlog.CREDATE), 1) * 100000000000 + DayOfYear(Partlog.CREDATE) * 100000000 + Hour(Partlog.CRETIME) * 1000000 + Minute(Partlog.CRETIME) * 10000 + Second(Partlog.CRETIME) * 100 + Hundredth(Partlog.CRETIME)
	
5/7/25: Add back fields such as name and ticket location to tags for organization. Solution: remade ‘new tags’ by replacing previous key barcode with timekey barcode.
On record duplication it had an error because it duplicated the timekey from the previous record. Changed from creation formula to modification formula, was able to keep unique flag selected. 
	
Statusupdate:
