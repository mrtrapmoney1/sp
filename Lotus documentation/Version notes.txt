<<<<<<<<<<<<<<<<<<<<<DISTINCT lOCATIONS>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

7/30 ADD A TEXT FIELD TO SHADOW THE BOOLEAN STOCK FIELD AND LINK -- AM UNABLE TO LINK STOCK FIELD TO MARCONE AVAILABLE RETURNS. DK
Tried 8/3 failed, linked stocktext as 1 charachter text field to stock checkbox but data didn't relate to that is distinctlocations.dbf not sure why this link doesn't work

<<<<<<<<<<<<<<<<<<Memo>>>>>>>>>>>>>>>>>>>>

9/22 Add Created time/date and mod time/date to memo field. Use ModDate to update the ModDate for the ticket OR modify so it also takes the moddate for the memo into account for dormant ticket find.

<<<<<<<<<<<<<<<<<Partlog:>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
	4/28/25 Added Doy of Year to the unique time field 

	Future: Add blue field with DOY on Bad Eyes DONE

	Trying to get leading zeros to show in datekey field 
	DOY x 100,000,000 
	hours x 1,000,000 
	minutes x 10,000 
	seconds x 100 
	hundredths x1 

	NEW FORMULA 4/29/25 
	DayOfYear(Partlog.CREDATE) * 100000000 + Hour(Partlog.CRETIME) * 1000000 + Minute(Partlog.CRETIME) * 10000 + Second(Partlog.CRETIME) * 100 + Hundredth(Partlog.CRETIME) 

	DEPRECIATED FORMULA 4/29/25 
	Copied existing formula for datekey  
	Combine(DayOfYear(Partlog.CREDATE), Hour((Partlog.CRETIME)), Minute((Partlog.CRETIME)), Second((Partlog.CRETIME)), Hundredth((Partlog.CRETIME))) 

	5/1 Change DateKey to Numeric 12.0 Modification FORMULA, added last digit of year to string,  
	 
	5/2 Made TimeKey to be unique and filled in. Removed unique requirement for cretime field.  
	 
	Current formula for TimeKey field is: Right(Year(Partlog.CREDATE), 1) * 100000000000 + DayOfYear(Partlog.CREDATE) * 100000000 + Hour(Partlog.CRETIME) * 1000000 + Minute(Partlog.CRETIME) * 10000 + Second(Partlog.CRETIME) * 100 + Hundredth(Partlog.CRETIME) 
	 
5/7/25: Add back fields such as name and ticket location to tags for organization. Solution: remade new tags by replacing previous key barcode with timekey barcode. 
On record duplication it had an error because it duplicated the timekey from the previous record. Changed from creation formula to modification formula, was able to keep unique flag selected.  

6/19 Was getting duplicate TimeKey entries on a single order. Changed timekey formula to remove hundredths of a second and sub in a random 2 digit number instead. New formula is 
Right(Year(Partlog.CREDATE), 1) * 100000000000 + DayOfYear(Partlog.CREDATE) * 100000000 + Hour(Partlog.CRETIME) * 1000000 + Minute(Partlog.CRETIME) * 10000 + Second(Partlog.CRETIME) * 100 + (Random() * 99)
6/19 Changed formula so it's not tied to CreDate and CreTime
Right(Year(Today()), 1) * 100000000000 + DayOfYear(Today()) * 100000000 + Hour(CurrTime()) * 1000000 + Minute(CurrTime()) * 10000 + Second(CurrTime()) * 100 + (Random() * 99) [revision 2609]

7/10 attempting to get partlog key to work with marcone available returns
;Before
Combine("MARCONE AVAILABLE RETURNS".PRICE, '  ', "MARCONE AVAILABLE RETURNS".INVOICE_, '  ', "MARCONE AVAILABLE RETURNS".PART)
;After
Combine (Trim(MARCONE AVAILABLE RETURNS.PRICE), '  ', Trim(MARCONE AVAILABLE RETURNS".INVOICE), '  ', Trim(MARCONE AVAILABLE RETURNS".PART))

;Before
Combine(Trim(Partlog.COST), '  ', Trim(Partlog.MAN_INVOIC), '  ', Trim(Partlog.PART_NUM))
;After
Combine (Trim(Partlog.COST), '  ', Trim(Partlog.MAN_INVOIC), '  ', Trim(Partlog.PART_NUM))

7/10 Changed key for partlog to include narda number. Made key for Marcone Sales and Credit.apr to match the one in partlog. pasted prior formula for partlog.key to the memo of the first ticket. Added .1 to all the po numbers after 01/31/25 to trigger the key change since it's a modification formula. Will have to undo that after all key changes have been completed. DONE

7/10 Later I'll change the partlog.key back to original formula and trigger the modification formula for all applicable records. Then it will sync up with the marcone credits.apr file. DONE

8/22 Need to add resolved part number field, doesn't need to be permanently visible but see stock only.apr for guidance. Need to be able to find all versions upstairs for use. dk 
Linked ResolvedPn on Stock only.dbf and 9999.dbf DONE

For example, to find records in an Orders database that correspond to sales over $1000 in either Japan or the US,
build the Find object as follows:
Dim SalesFind As New Find
Call SalesFind.And (OrderTotal, "> 1000")
Call SalesFind.And (Country, "Japan")
Call SalesFind.Or (OrderTotal, "> 1000")
Call SalesFind.And (Country, "USA")
The OrderTotal condition must be repeated to find the proper set of records




	 
<<<<<<<<<<<<<<<<<<<<<<<<<<<Statusupdate: >>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

5/14/25
C:\Users\metro\Backups\Lotus 2025-05-14 02;00;01 (Full)\STATUSUPDATE.APR
Created a part number lookup with 1st row P1 and PD1

5/20 Figured out linking of part export to statusupdate. Databases need to be linked by model field.

6/10 See C:\Users\metro\Backups\Lotus 2025-06-10 02;00;01 (Full)\STATUSUPDATE.APR for new add memo functions.
6/11 added double CR to memo entry formula, Combine(memo.MEMO, Chr(13), Chr(10), Chr(13), Chr(10), Today(), ' ', VarMake, ' ', VarPN)

6/30 Need to fix DateCompl is invalid message on duplication of ticket.
Formula appears to be correct. CUSTDATA.DATECOMPLT = '' or (CUSTDATA.DATECOMPLT <= Today() and CUSTDATA.DATECOMPLT >= CUSTDATA.DATEIN)
Dup2 macro is corrupted. FocSympt script is not available, runs Hide11, from partlog, instead. June 1 and June 15 backups are fine. Check June 22 backup. June 22 backup works. Check June 27th. Backup from June 28th was good and was used as /lotus/statusupdate.apr ongoing.

7/21 Current Validation formluls for CUSTDATA.DATECOMPLT is CUSTDATA.DATECOMPLT = '' or (CUSTDATA.DATECOMPLT <= Today() and CUSTDATA.DATECOMPLT >= CUSTDATA.DATEIN) which is causing problem on ticket duplication. Need to run a macro after ticket is duplicated to remove inappropriate CUSTDATA.DATECOMPLT entries.

DistinctLocations.apr
Add field for each location so the percent full  can be indicated in 10% increments from dropdown list. DONE

7/29 Change Assurant/Lowes Ext to set btname btaddress hubid to Assurant values then focus at btname and if that is Assurant it would end the macro and if that is Assurant/Lowe's Ext then it would run the remainder of the macro as Assurant/Lowe's Ext

8/5 Changed how the macros work when making a new ticket. Macros are selected based on the data in 'btname' rather than running sequentially. If statements have been removed from relevent macros. DONE

<<<<<<<<<<<<<<<<<<<<<<<<Stock only>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
Attempted to resolve part number to the superpart in cases only where a superpart number was present with if isblank(stock only:resovedpn, ) DONE


PNPDCost
7/23 Change so records can be added by filling in price from the partlog. (Will create a new complete record in pnpdcost database.)
7/23 Add non-claimable field to database so we have a per part number checkbox on partlog Roya vetoed


MySQL
https://www.youtube.com/watch?v=zRfI79BHf3k&t=56s
installed MySQL on 192.168.1.30 default settings using password on barcode. Attempted to use settings with password to connect remotely on LAN but failed. 
Solution: first you have to enter the password for the Ubuntu machine, then you have to enter the password for MySQL.

<<<<<<<<<<<<<<<<<<<<<<<<<TO Do>>>>>>>>>>>>>>>>>>>>>
Statusupdate.apr Make a combined field for the make, mn and sn on the tag at bottom left.
Fix bug in Marcone credits.apr so that key field forms correctly. 

Explore more scripting oppurtunities
Example: Sub ltrt
	CurrentApplication.ApplicationWindow.domenucommand(IDM_TILE_LEFT_RIGHT)
End Sub
This script splits the lotus application to left and right windows.